<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <!-- Root aggregator/pom: no longer inherits from spring-boot-starter-parent so
         only modules that declare Spring Boot dependencies or the plugin will
         actually be Spring Boot apps. Versions for Spring Boot are provided via
         the BOM below in dependencyManagement. -->
    <groupId>pe.com.relari.fwk.spring</groupId>
    <artifactId>relari-spring-module</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>relari-spring-module</name>
    <description>Employee Management API</description>
    <modules>
        <module>audit</module>
        <module>handler-error-spring</module>
        <module>handler-error-spring-reactive</module>
        <module>support-spring</module>
        <module>web</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
<!--        <sonar.projectKey>api-employee-devops</sonar.projectKey>-->
<!--        <sonar.projectName>api-employee-devops</sonar.projectName>-->

        <!-- For SonarCloud -->
<!--        <sonar.organization>relari</sonar.organization>-->
<!--        <sonar.host.url>https://sonarcloud.io</sonar.host.url>-->
<!--        <sonar.token>5a7d3aea33719671810456e3ebdac6abfe76b02b</sonar.token>-->

        <!-- For Local SonarQube -->
<!--        <sonar.organization>${project.groupId}:${project.artifactId}</sonar.organization>-->
<!--        <sonar.host.url>http://localhost:9000</sonar.host.url>-->
<!--        <sonar.token>sqa_a872748770a8788b2c31b3f3f373f0ca6797ceb9</sonar.token>-->
        <sonar.coverage.jacoco.xmlReportPaths>${project.build.directory}/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>
        <sonar.junit.reportPaths>${project.build.directory}/surefire-reports</sonar.junit.reportPaths>
        <sonar.exclusions>
            **/EmployeeApplication.*,
            **/annotations/**,
            **/config/**,
            **/exception/**,
            **/model/**,
            **/util/**
        </sonar.exclusions>
        <org.mapstruct.version>1.5.5.Final</org.mapstruct.version>
        <retrofit.version>2.9.0</retrofit.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>3.5.16</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <version>2.0</version>
        </dependency>

        <!-- https://mvnrepository.com/artifact/org.springdoc/springdoc-openapi-starter-webmvc-ui -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.13</version>
        </dependency>

        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${org.mapstruct.version}</version>
        </dependency>

        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct-processor</artifactId>
            <version>${org.mapstruct.version}</version>
        </dependency>

        <dependency>
            <groupId>pe.com.relari.commons</groupId>
            <artifactId>commons-utils</artifactId>
            <version>1.0.0-SNAPSHOT</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.sonarsource.scanner.maven</groupId>
                <artifactId>sonar-maven-plugin</artifactId>
                <version>5.5.0.6356</version>
            </plugin>
        </plugins>
    </build>

</project>
